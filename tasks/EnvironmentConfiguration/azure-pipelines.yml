name: "3.1.$(rev:r)"

trigger:
  batch: true
  branches:
    include:
      - "*"
  paths:
    include:
      - tasks/EnvironmentConfiguration
      - scripts/Build-Task.ps1

pr: none

workspace:
  clean: all

pool:
  name: 'DAS - Continuous Integration'
  demands: Agent.OS -equals Windows_NT

steps:
# build and test extension
- task: PowerShell@2
  displayName: Build Task
  inputs:
    filePath: $(System.DefaultWorkingDirectory)/scripts/Build-Task.ps1
    arguments:  -Build -TaskRoot $(System.DefaultWorkingDirectory)/tasks/EnvironmentConfiguration -Verbose
    failOnStderr: false
    pwsh: false
    workingDirectory: $(System.DefaultWorkingDirectory)
- task: PowerShell@2
  displayName: Test
  inputs:
    filePath: $(System.DefaultWorkingDirectory)/EnvironmentConfiguration/Release/tests/Start-UnitTest.ps1
    failOnStderr: true
    pwsh: false
    workingDirectory: $(System.DefaultWorkingDirectory)
- task: PublishTestResults@2
  inputs:
    testResultsFormat: "NUnit"
    testResultsFiles: $(System.DefaultWorkingDirectory)/release/EnvironmentConfiguration/tests/Test-Pester.XML
  displayName: "Publish results"
  condition: succeededOrFailed()
# publish extension
- template: ../../pipeline-templates/step/extension-publish.yml
  parameters:
    BuildDirectory: $(System.DefaultWorkingDirectory)/release/EnvironmentConfiguration
    ExtensionName: EnvironmentConfiguration
